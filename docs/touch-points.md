# Upstream touch points

Every modified upstream file, with one line on why. Custom behavior belongs in new files;
this list is the budget (target: <10 entries at end state) and the merge-conflict map for
upstream updates.

| File | Why |
|---|---|
| `common/params_keys.h` | Tailscale param keys (12) + custom long/lat opt-in params (incl. `CurveMemoryEnabled`, `LeadAnticipationMode`, compatibility `LeadAnticipationEnabled`) + speed-aware torque params (2) |
| `selfdrive/controls/controlsd.py` | Opt-in lateral demand pipeline hook (fail-closed call on the model-curvature branch), steering-override context for guarded curve memory, path-evidence handoff for the Torque v2.1 under-response guard, plus observability-only `modelPathState` publication. REMAINING (harness-gated): wire lane-line y0 into `demand/wiring.build_pipeline_inputs` for lane-centering/DTLE refinement |
| `selfdrive/controls/lib/latcontrol_torque.py` | Pass `CS.vEgo` into the torque override extension hook (1 line) |
| `selfdrive/locationd/torqued.py` | Route accepted learning points through the extension hook, run shadow-only disturbance classification for observability, and persist speed profiles on the 60s cache cadence |
| `sunnypilot/selfdrive/controls/lib/longitudinal_planner.py` | Opt-in custom-2.0 longitudinal hook in `update_targets` (import + adapter + 1 fail-closed call). Model-stop now read from upstream `modelV2.action.shouldStop`/`desiredAcceleration` (+ predicted stop distance derived from the model trajectory `position.x`/`velocity.x`) + coast from `get_coast_accel(pitch)` inside the adapter (no new planner touch). DEC bypassed in `is_e2e`/`update` when custom long is on (SCC replaces it). Holds the §3 `LeadAnticipation` adapter |
| `selfdrive/controls/lib/longitudinal_planner.py` | §3 lead-motion anticipation: 1 fail-closed call (`self.lead_anticipation.shape(radarState, dt)`) with default-shadow/raw passthrough and explicit apply mode for confidence-shaping lead accel fed to `mpc.update` (returns the raw radarState when off/shadow/on fault) |
| `selfdrive/controls/lib/longitudinal_mpc_lib/long_mpc.py` | Decouple stop gap from moving follow distance: `STOP_DISTANCE` (6 m standstill buffer) now fades with speed to `MOVING_GAP` (1.5 m) via `follow_offset(v)` in `get_safe_obstacle_distance` (the acados cost). Cuts the low-/mid-speed hang-back; stop gap + time-gap unchanged. Regenerated by the SConscript on build |
| `selfdrive/ui/sunnypilot/layouts/settings/steering_sub_layouts/torque_settings.py` | Offroad speed-aware torque mode selector + dialog wiring |
| `selfdrive/ui/sunnypilot/layouts/settings/settings.py` | Mount consolidated schema-driven panels: `Driving` (steering+cruise) + `Interface` (visuals+display) + a global `Search` panel (jump-to-setting via `_navigate_to_setting`); sidebar collapses 4 entries → 2 and gains Search |
| `system/manager/process_config.py` | `manage_tailscaled` daemon process entry (2 lines) |
| `selfdrive/ui/sunnypilot/layouts/settings/developer.py` | Tailscale install/enable/login/logout settings items |
| `cereal/log.capnp` | Phase 0b shadow-only lateral disturbance counters on `LiveTorqueParametersData`; observability-only lateral `ControlsState.modelPathState` and Torque v2.1 `adaptiveTorqueState` telemetry |
| `sunnypilot/selfdrive/controls/controlsd_ext.py` | Dispatch torque v2.1 when `TorqueControlTune == 2.1` (import + 1 `elif`); hold the opt-in `LateralDemandAdapter` |
| `sunnypilot/selfdrive/controls/lib/latcontrol_torque_versions.json` | Register `v2.1` in the native torque-selector manifest |
| `selfdrive/controls/radard.py` | Cut-in override: 1 fail-closed call (`apply_cut_in_override`) wrapping `get_lead()` for `leadOne` — promotes high-risk on-path radar tracks (closing speed ≥ 2 m/s, TTC ≤ 8 s, on-path, moving, persistent) to `leadOne` when the vision model hasn't confirmed them. Gated by `CustomLongitudinalEnabled`; never overrides a confirmed lead |
